# Required for durable sessions. Without it, the API uses in-memory demo storage.
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/?
MONGO_DB=motor_signal
# Comma-separated deployed frontend URLs. Add localhost too if you develop locally.
CORS_ORIGINS=https://your-project.vercel.app,http://localhost:5173
